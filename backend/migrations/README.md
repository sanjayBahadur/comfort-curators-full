# Migrations

Forward-only PostgreSQL migrations are registered here by the migration
foundation task. This directory is intentionally present so launcher phase
checks fail loudly until the migration runner is implemented.

