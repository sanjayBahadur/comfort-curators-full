Format: `DATE | BLOCK | decision — reason`

```
2026-08-09 | P0 | Rename work dispatched via .sandcastle/ (opencode-go, DeepSeek V4 Pro for Tier 1), not this repo's existing Gastown rig — human's explicit choice after being shown the tradeoff (Gastown is this repo's stated standard, "no custom OpenCode launcher," and already built the existing V0 backend; sandcastle duplicates it but was already scaffolded and is what the frontend repo's ORCHESTRATION.md assumed uniformly)
2026-08-09 | P0 | Sandbox has no Docker-in-Docker — the agent runs go build/go vet/go test (unit-level) to self-check; full `make phase-N` (needs Compose + Postgres) runs on the host after merge, matching AGENTS.md's own model of phase verification happening outside the provider session
2026-08-09 | P0 | Stashed a pre-existing uncommitted fix (ConfirmMFA atomic-audit-write) found on main before branching — not part of this rename, not discarded, see git stash list
2026-08-09 | P0.4 | HUMAN SIGN-OFF RECORDED: contracts/ edit authorized for P0.4 only, AGENTS.md restriction lifted for this block. Human reviewed the exact 8-line diff plan across 4 files (not a blank authorization) and decided: (1) AgentRunCreate.run_kind enum becomes [superhost, hermes], jarvis dropped from valid input; (2) /v1/jarvis/runs kept in the OpenAPI spec as a documented deprecated 308-redirect entry, not silently removed, since it's still live for one release per P0.3
2026-08-09 | P3.0 | HUMAN SIGN-OFF RECORDED: contracts/api/superhost-stream.yaml creation authorized. Human approved the proposed shape as presented (not a blank authorization): SSE stream on GET /v1/superhost/threads/{id}/stream emits AgentRunEvent-shaped records (matching the existing agent_run_events table/event-name convention already in code), with new versioned event names for the tool-call flow — ToolCallProposed.v1, PolicyDenied.v1, ApprovalRequired.v1, PolicyAllowed.v1, RunCompleted.v1 (reusing existing completion events) — each carrying tool name, policy decision, and reason in event_data
```
